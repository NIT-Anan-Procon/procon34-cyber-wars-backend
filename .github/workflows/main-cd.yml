name: main CD
on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  main-cd:
    runs-on: ubuntu-latest
    steps:
      - name: CD
        uses: ./.github/actions/cd
        with:
          branch-name: main
